#include <cpp_core/interface/get_version.h>

// Keep the inline C API definition in a library translation unit so Windows
// linkers emit the exported getVersion symbol because windows is a bit picky and stupid sometimes.
